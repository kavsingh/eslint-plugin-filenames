{
	// tsconfig for actually building only includes src
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"noEmit": false,
		"module": "NodeNext",
		"rootDir": "src",
		"outDir": "dist"
	},
	"include": ["src/**/*.ts", "src/**/*.cts"]
}
